# Xcode
#
# .DS_Store and other system files
.DS_Store
UserInterfaceState.xcuserstate

# Xcode project workspace and schemes
xcuserdata/
*.xcworkspace
*.xcuserstate
*.xcodeproj/project.xcworkspace/
*.xcodeproj/xcuserdata/

# Swift Package Manager
#
# Add this line if you have `Package.resolved` files generated by SwiftPM
# when you add dependencies using the Swift Package Manager.
Package.resolved

# CocoaPods
#
# We recommend against adding the Pods directory to your .gitignore. However
# you should judge for yourself, the pros and cons are described at:
# https://guides.cocoapods.org/using/using-cocoapods.html
Pods/
# Add this line if you want to avoid checking in source control of your
# `Pods` directory (default behavior).

# Carthage
#
# Carthage/Build/
# Comment the line above if you're using submodules.

# Fastlane
#
# It is recommended to not store the screenshots in the git repo.
# Instead, use fastlane to re-generate the screenshots whenever they are needed.
fastlane/report.xml
fastlane/screenshots/**/*.png
fastlane/test_output/**/*.log
fastlane/Preview.html
fastlane/screenshots/**/*.png

# Bundle artifacts
*.ipa
*.dSYM.zip
*.dSYM

# SwiftPM
.build/

# Xcode Generated Metadata Files
*.xcscmblueprint
